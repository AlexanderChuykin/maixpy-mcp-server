# 🐳 Docker установка MaixPy MCP Server

## Быстрый старт с Docker

### Вариант 1: Автоматический запуск

```powershell
# Windows
.\docker-start.bat

# Linux/macOS
chmod +x docker-start.sh
./docker-start.sh
```

### Вариант 2: Вручную

```powershell
# Сборка образа
docker-compose build

# Запуск контейнера
docker-compose up -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f
```

---

## Настройка VS Code

После запуска Docker контейнера добавьте в `settings.json`:

**Windows:** `%APPDATA%\Code\User\settings.json`

```json
{
  "github.copilot.chat.mcp.servers": {
    "maixpy-docs": {
      "url": "http://localhost:8000/sse"
    }
  }
}
```

**Важно:** Используется `url` вместо `command` - подключение через HTTP!

---

## Проверка работы

### 1. Проверка контейнера

```powershell
# Статус контейнера
docker-compose ps

# Логи сервера
docker-compose logs maixpy-mcp

# Должны увидеть:
# 🚀 Запуск MCP сервера в HTTP режиме на 0.0.0.0:8000
```

### 2. Проверка HTTP endpoint

```powershell
# Windows PowerShell
Invoke-WebRequest http://localhost:8000/sse

# Linux/macOS
curl http://localhost:8000/sse
```

### 3. Проверка в VS Code

1. Перезапустите VS Code
2. Откройте Copilot Chat
3. Введите:
   ```
   @workspace Список всех модулей MaixPy v4
   ```

---

## Управление контейнером

```powershell
# Остановка
docker-compose stop

# Запуск
docker-compose start

# Перезапуск
docker-compose restart

# Просмотр логов в реальном времени
docker-compose logs -f

# Остановка и удаление
docker-compose down

# Пересборка после изменений
docker-compose build --no-cache
docker-compose up -d
```

---

## Особенности Docker версии

### ✅ Преимущества

- **Изолированность** - не влияет на систему
- **Портативность** - работает везде где есть Docker
- **Простое обновление** - пересборка образа
- **Одинаковое окружение** - у всех одна версия
- **Легкая отладка** - просмотр логов через docker-compose

### 📦 Что в контейнере

- Python 3.11
- MCP SDK и все зависимости
- Сервер MaixPy documentation
- Вся документация MaixPy v4

### 🔧 Переменные окружения

```yaml
# В docker-compose.yml
environment:
  - MCP_HOST=0.0.0.0    # Адрес для привязки
  - MCP_PORT=8000       # Порт сервера
```

### 📁 Volume монтирование

Документация монтируется как volume:
```yaml
volumes:
  - ./doc_md:/app/doc_md:ro
```

Это позволяет обновлять документацию без пересборки контейнера!

---

## Использование с удаленным сервером

Если сервер на другой машине:

```json
{
  "github.copilot.chat.mcp.servers": {
    "maixpy-docs": {
      "url": "http://192.168.1.100:8000/sse"
    }
  }
}
```

### Безопасность для продакшена

Для публичного доступа рекомендуется:

1. **Использовать обратный прокси (nginx):**
   ```nginx
   location /mcp {
       proxy_pass http://localhost:8000;
       proxy_http_version 1.1;
       proxy_set_header Connection "";
   }
   ```

2. **Добавить SSL/TLS:**
   ```json
   {
     "url": "https://your-domain.com/mcp/sse"
   }
   ```

3. **Настроить аутентификацию** (в будущих версиях)

---

## Отладка

### Проблема: Контейнер не запускается

```powershell
# Проверка логов
docker-compose logs maixpy-mcp

# Проверка образа
docker images | findstr maixpy

# Пересборка
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### Проблема: VS Code не подключается

1. Проверьте, что контейнер запущен:
   ```powershell
   docker-compose ps
   ```

2. Проверьте доступность endpoint:
   ```powershell
   curl http://localhost:8000/sse
   ```

3. Проверьте URL в settings.json (должен быть `http://localhost:8000/sse`)

4. Перезапустите VS Code

### Проблема: Порт 8000 занят

Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8080:8000"  # Внешний порт 8080
```

И в VS Code:
```json
{
  "url": "http://localhost:8080/sse"
}
```

---

## Обновление документации

Документация монтирована как volume, поэтому:

```powershell
# 1. Обновите файлы в ./doc_md/

# 2. Перезапустите контейнер
docker-compose restart

# Пересборка НЕ требуется!
```

---

## Требования

- Docker Desktop (Windows/macOS) или Docker Engine (Linux)
- Docker Compose
- 500 MB свободного места
- VS Code с GitHub Copilot

---

## Производительность

Типичное использование:
- **RAM:** ~200 MB
- **CPU:** <5% (в покое)
- **Disk:** ~300 MB (образ)

---

## Сравнение с локальной версией

| Аспект | Локально (stdio) | Docker (HTTP) |
|--------|------------------|---------------|
| Установка | pip install | docker-compose up |
| Запуск | Автоматически VS Code | Отдельный контейнер |
| Порты | Не требуются | 8000 |
| Удаленный доступ | ❌ Нет | ✅ Да |
| Изоляция | ❌ Нет | ✅ Да |
| Обновление | pip upgrade | docker-compose build |

---

**Готово!** 🚀 

Сервер работает в Docker, подключение через URL.
